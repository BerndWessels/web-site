/**
 * This is the authorization module configuration.
 * Do NOT configure routes in this file.
 * @memberOf Authorization Module
 * @namespace Authorization Config
 */
(function () {
  'use strict';

  // Register the module's configuration.
  angular.module('app.authorization').config(config);

  // Inject the dependencies.
  config.$inject = [];

  /**
   * @name config
   * @desc Configures the module.
   * @memberOf Authorization Module.Authorization Config
   */
  function config() {
  }

})();
